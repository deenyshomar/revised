#include<stdio.h>
/**
 * Description: for loop, conditional statement
 *
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 **/
int main(void)
{
	int i = 10;

	//for (initialize things; loop if this is true; do this after each loop)
	
	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
